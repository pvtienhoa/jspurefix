import { IJsFixConfig } from '../../config';
import { MakeFixSession } from '../make-fixl-session';
export declare function httpInitiator(config: IJsFixConfig, sessionFactory: MakeFixSession): Promise<any>;
